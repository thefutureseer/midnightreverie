We want to set up End-to-End (E2E) UI testing for Midnight Reverie using Playwright. Please implement the setup according to industry best practices, ensuring it runs smoothly in this Replit environment.

Please execute the following tasks sequentially:

1. Installation and Configuration:
   - Install the latest version of Playwright (`@playwright/test`).
   - Run the necessary browser installations so headless Chromium is available for tests.
   - Create a `playwright.config.js` file in the root directory. Configure it to run tests located in a `tests/e2e` directory, set a reasonable timeout (e.g., 30000ms), and configure it to automatically spin up our local frontend server (using the 'webServer' property) before running the tests.

2. Clean Boilerplate & Project Structure:
   - Ensure a clean `tests/e2e` directory structure. Delete any default sample tests (like `example.spec.js`) if they are generated by default.
   - Create a new test file named `tests/e2e/sellOutTrigger.spec.js`.

3. Write the "Sell-Out Trigger" E2E Test:
   - Inside `sellOutTrigger.spec.js`, write a test scenario that simulates the virtual ticket unlock flow.
   - The test should:
     a. Navigate to a mock event details page.
     b. Verify that the "Buy Virtual Ticket" button is initially disabled (or replaced by "Join Virtual Waitlist") because physical seats are still available.
     c. Simulate a user clicking the button to buy the remaining physical tickets.
     d. Assert that the UI dynamically updates to show the "Buy Virtual Ticket" button as enabled/active once the physical capacity hits 100%.

4. Repository Hygiene (.gitignore):
   - Update our root `.gitignore` file to ensure no Playwright test artifacts are tracked by Git. 
   - Specifically add: `test-results/`, `playwright-report/`, `blob-report/`, and `playwright/.local-browsers/`.

When completed, please run the test once using `npx playwright test` to verify that the environment is fully operational and the test suite builds without compiling errors.